[tools]
node = "24.14.1"
pnpm = "10.33.0"
"npm:npm-check-updates" = "19.6.5"
"github:biomejs/biome" = { version = "2.4.8", version_prefix = "@biomejs/biome@" }
lefthook = "2.1.4"
infisical = "0.43.60"
cocogitto = "7.0.0"

[tasks.setup]
description = "puts everything in its place"
run = ["mise trust --yes", "mise install", "pnpm install", "lefthook install"]
[tasks.dev]
description = "runs in watch mode"
run = "infisical run -- node --watch src/main.ts | npx pino-pretty"
[tasks.start]
description = "runs it for real"
run = "infisical run -- node src/main.ts"
[tasks.lint]
description = "attempts to fix what is safe to do"
run = "mise lint-check --write"
[tasks.lint-check]
description = "lint and format check with biome"
run = "biome check --diagnostic-level=warn --no-errors-on-unmatched"
[tasks.typecheck]
description = "checks entire project for type errors"
run = "pnpm tsc"
[tasks.typecheck-staged]
description = "checks passed/staged files for type errors"
run = "pnpm tsc-files"
[tasks.test]
description = "runs all tests"
run = "pnpm vitest"
[tasks.test-staged]
description = "runs tests on passed/staged files"
run = "pnpm vitest related --bail=1 --reporter=github-actions"
[tasks.test-ci]
description = "runs tests silently with quick fail"
run = "pnpm vitest --bail=1 --reporter=github-actions"
[tasks.register]
description = "registers commands, --dev for dev server, no flag for global"
run = "infisical run -- node ./src/registration.ts"
[tasks.unregister]
description = "unregisters commands, --dev for dev server, no flag for global"
run = "infisical run -- node ./src/registration.ts --unregister"

[settings]
disable_backends = ["asdf"]
use_versions_host = false
